# Copy to .env and set your key to enable real Claude digests.
# Without it, the server uses a deterministic fallback digest.
ANTHROPIC_API_KEY=
# Optional: override the API port (default 8787)
PORT=8787
